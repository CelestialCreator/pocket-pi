{
  "name": "pi-termux-tools",
  "version": "0.1.0",
  "description": "Pi extension exposing Termux:API as agent tools — camera, TTS, notifications, share, location, sensors, clipboard. Designed for Pocket Pi but usable on any Termux install.",
  "license": "MIT",
  "author": "Pocket Pi contributors",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "keywords": ["pi-package", "pi", "extension", "termux", "android"],
  "pi": {
    "extensions": ["dist/index.js"]
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
